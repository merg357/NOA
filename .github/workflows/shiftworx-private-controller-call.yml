name: shiftworx-private-controller-call

on:
  push:
    branches: [main]
    paths:
      - '.ci/shiftworx-private-controller-call-trigger.md'

permissions:
  contents: read

jobs:
  private-controller-probe:
    uses: merg357/patternai/.github/workflows/shiftworx-private-controller-probe.yml@main
